{
  "name": "mongodb-event-store",
  "version": "0.0.0-alpha",
  "description": "Event store for MongoDB.",
  "license": "MIT",
  "devDependencies": {
    "editorconfig-tools": "0.1.1",
    "husky": "0.14.3",
    "snazzy": "7.0.0",
    "standard": "10.0.3"
  },
  "scripts": {
    "lint": "standard --verbose | snazzy",
    "precommit": "echo \"Warning: no precommit specified\" && exit 0",
    "prepush": "echo \"Warning: no prepush specified\" && exit 0"
  }
}
